import { ComputerOptimizedSelectionDirective } from './computer-optimized-selection.directive';

describe('ComputerOptimizedSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ComputerOptimizedSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
