import { ComputerRandomSelectionDirective } from './computer-random-selection.directive';

describe('ComputerRandomSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ComputerRandomSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
